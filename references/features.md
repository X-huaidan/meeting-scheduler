# 功能清单

本文件列出孟总会务助手当前具备的所有功能，方便你增加、删除、修改。

> **修改约定**：每个功能用 `## X.Y 功能名` 组织。删除功能直接删除对应章节；新增功能按编号顺序追加；调整功能改对应章节内容。

---

## 1. 会议录入

### 1.1 文字排会

用户用自然语言或结构化文字描述会议，agent 解析后写入行程表。

**触发词**：帮我安排会议 / 把这个会议加进行程表 / 排会

**输入示例**：
- "今天 11:20-12:00 跟邓在 302 开会，讨论三季度战略"
- "周三下午两点开组织生活会，议程：1.学习 2.讨论 3.总结"

**输出流程**：读取结构 → 识别字段 → 给预览 → 用户确认 → 写入 → 回读校验

### 1.2 截图排会

用户发送企业微信截图、会议通知截图，agent 识别后写入。

**触发词**：截图排会 / 这个会议加进去（带图片）

**支持来源**：企业微信、微信、邮件、任意可读图片

**识别字段**：会议标题、时间、地点、参会领导、参会人员、议程

### 1.3 多会议批量排

一次输入多个会议（如一段话里说了 3 场），agent 自动拆分并分别写入。

**触发词**：今天有这些会议 / 这周安排

---

## 2. 会议读取

### 2.1 读取当前行程表

读取行程表的当前内容并展示给用户。

**触发词**：今天有什么会议 / 周三的行程 / 看看行程表

### 2.2 检测时间冲突

同一时间段有多个会议时自动标红/提示。

**触发词**：检查冲突 / 有没有撞会

---

## 3. 版面整理

### 3.1 应用底色

按"排会规则 A.1"给指定行填底色。

**触发词**：填底色 / 调色 / 标色

### 3.2 应用对齐

按"排会规则 A.2"给指定列设对齐方式。

**触发词**：调整对齐 / 左对齐 / 居中

### 3.3 应用换行

按"排会规则 A.3"给指定列设自动换行。

**触发词**：自动换行 / 换行

### 3.4 隐藏空白行

按"排会规则 B.8"把指定天的空白行隐藏。

**触发词**：整理版面 / 隐藏空白行 / 把周五整理一下

### 3.5 展开所有行

把指定天/全表所有隐藏的行展开（恢复可见）。

**触发词**：展开 / 显示所有行 / 取消隐藏

### 3.6 还原误改行高

恢复被 `setRowHeightsForced` 误改的行高（最高到 45pt）。

**触发词**：恢复行高 / 还原 R56

---

## 4. 表格结构识别

### 4.1 自动识别每周标记行

根据 B 列内容（"周一"/"周二"/...）自动识别每天的起始行。

### 4.2 自动识别午休分隔行

根据行高（9~20pt）自动识别午休分隔行。

### 4.3 自动识别当天行范围

基于标记行 + 午休分隔行 + 下一个标记行，自动算出当天的所有行号范围。

### 4.4 自动识别哪几行有会议

根据 C 列时间字段非空判断，区分"会议行"和"空白行"。

---

## 5. 跨设备同步

### 5.1 自动打包

技能更新后自动打包为 zip。

**触发词**：打包 / 打包成 zip

**实现**：`scripts/publish_to_baidu.py` 内部调用 `package_skill.py`

### 5.2 自动发布到百度网盘

每次技能更新后自动上传到 `/软件/WorkBuddy/`。

**触发词**：上传网盘 / 打包上传 / 发布

**当前状态**：✅ 已创建网盘目录 `/软件/WorkBuddy/`（fs_id: 950876063256874）

### 5.3 Git 同步（公司电脑 → 家里电脑）

公司电脑技能修改后，commit + push 到 GitHub；家里电脑 `git pull` 同步。

**仓库**：https://github.com/X-huaidan/meeting-scheduler.git

**当前状态**：✅ 已 push v1.0.0（commit `9bbe36b`）

### 5.4 版本管理

- 语义化版本号（X.Y.Z）+ 日期后缀
- 每次发布自动递增
- 历史版本可回滚（`git checkout v1.0.0`）

---

## 6. 工具脚本

### 6.1 `scripts/cdp.py`

CDP 客户端，通过 websocket-client 接管用户 Chrome 浏览器。

**能力**：
- `CDP.send(method, params)` 调任意 CDP 命令
- `CDP.eval(expression)` 在页面执行 JS
- `get_page_ws(port)` 获取目标页面的 ws URL

### 6.2 `scripts/sheet_ui.py`

sheet UI 操作封装，常用坐标、点击、按键函数。

**能力**：
- `click(x, y)`：真实鼠标点击
- `key(key)`：模拟键盘按键
- `snapshot(path)`：截图
- 名称框跳转、整行选中、右键菜单等场景封装

### 6.3 `scripts/publish_to_baidu.py`

自动发布脚本（每天 23:59 自动跑，或用户手动触发）。

**流程**：检测变化 → 打包 → 部署 EdgeOne → 转存百度网盘 → 写 CHANGELOG

**当前限制**：因 WorkBuddy 沙箱代理挡 GitHub HTTPS（实际未跑通），**当前流程只完成到打包**，跨设备同步靠 Git（5.3）。

### 6.4 `scripts/decode_to_zip.py`（计划中）

base64 → zip 还原脚本。**当前未实现**——因为有 Git 方案后不再需要 base64 兜底。

---

## 7. 自我管理

### 7.1 展示排会规则

**触发词**：展示排会规则 / 看看规则 / 排会规则 / 显示规则

**输出**：完整读取并展示 `references/rules.md` 内容

### 7.2 展示功能列表

**触发词**：展示功能 / 看看功能 / 功能清单 / 这个技能能干啥

**输出**：完整读取并展示 `references/features.md` 内容（本文件）

### 7.3 增加规则

**触发词**：增加一条规则 / 加个规则 / 新规则

**流程**：用户描述新规则 → agent 写入 `rules.md` 对应章节 → 提示修改位置

### 7.4 删除规则

**触发词**：删除规则 / 删掉 X 规则 / 不需要这条

**流程**：用户指定规则名 → agent 从 `rules.md` 删除对应行 → 提示删除位置

### 7.5 增加功能

**触发词**：增加一个功能 / 新功能 / 加功能

**流程**：用户描述功能 → agent 写入 `features.md` 对应章节 → 提示修改位置

### 7.6 删除功能

**触发词**：删除功能 / 不要这个功能 / 去掉 X 功能

**流程**：用户指定功能名 → agent 从 `features.md` 删除对应章节 → 提示删除位置

---

## 功能开关（计划中）

以下功能未启用，预留位置。**如果以后不需要，可以直接从这份清单删除。**

- **F.1 钉钉/飞书集成**：从其他办公工具自动同步会议 → 当前未启用
- **F.2 多 sheet 支持**：除了"时间表"，也支持"时间表2" → 当前仅支持"时间表"
- **F.3 周报自动生成**：基于一周会议自动生成周报 → 当前未启用
- **F.4 会议提醒**：提前 N 分钟弹窗提醒 → 当前未启用

---

## 修改记录

- 2026-09-11：v1.0.0 初始化功能清单
