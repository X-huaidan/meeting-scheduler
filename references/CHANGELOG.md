# 孟总会务助手 — 更新日志

> 用户说"展示更新日志 / 看更新日志 / 看变更历史 / 变更记录" → 完整显示本文档（不要总结、不要精简）。

---

## 已发布

### v1.2.0 (2026-09-14)

- **新增** `references/features.md` 第 8 章"周期维护"，新增 8.1 节：周日自动清理上周数据 + 展开隐藏行
  - 范围：本周一到本周五（C:I 清空 + 取消隐藏）
  - 保留：B 列标记行、午休分隔行
  - 安全约束：执行前必须列清单+用户确认（破坏性操作）
  - SKILL.md 触发词："清理本周 / 跑一次清理 / 模拟周日清理"
  - 待用户验证手动跑一次没问题后，再讨论自动触发方案
- **新增** `references/features.md` 第 7 章新增 7.7 节：发版完成汇报 + 技能大小预警
  - `scripts/publish_to_baidu.py` 加 `--size` 子命令 + `compute_size()` 函数
  - 阈值表（基于 LLM 加载 + 文件上传实务）：🟢 健康<50KB / 🟢 良好 50-100KB / 🟡 警戒 100-200KB / 🟠 警告 200-500KB / 🔴 危险>500KB
  - SKILL.md 同步：触发词+"触发 A 第 7 步发版汇报"+ 工具脚本清单
  - 当前实测：技能 55.83 KB（9 文件），SKILL.md 16.39 KB → 🟢 良好
- **重构** 发布策略：编辑即时落盘，发版批量进行
  - 加/删规则/功能不再每次自动 push，只 Edit 文档 + 追加 CHANGELOG"待发布"草稿
  - 发版改为两个触发条件：用户说"更新上传 / 打包上传" 或 每天 23:59 automation 自动跑
  - 新增 `references/CHANGELOG.md`（"已发布" + "待发布"两段），方便追溯每次变更
  - publish 脚本增加 `--show-draft` 和 `--bump-preview` 子命令
- **SKILL.md** 同步：调整"何时触发"和"版本管理与自动发布"章节，明确"编辑即落盘，发版批量进行"

### v1.1.0 (2026-09-11)

- **新增** `references/rules.md`（排会规则）和 `references/features.md`（功能清单）两个独立文档
- 规则 5 类：表格操作 / 会议安排 / 输入处理 / 工作流 / 自定义
- 功能 7 类：会议录入 / 会议读取 / 版面整理 / 表格结构 / 跨设备同步 / 工具脚本 / 自我管理
- SKILL.md 增加触发词："展示排会规则 / 展示功能 / 加/删规则 / 加/删功能"
- 触发后 agent 完整 Read 并显示对应文档

### v1.0.0 (2026-09-11)

- 初始版本
- 技能目录 `meeting-scheduler`（中文别名"孟总会务助手"）
- 排会核心功能：文字/截图识别 → 单元格写入 → 底色/对齐/换行 → 隐藏空白行
- CDP 接管 Chrome 实现隐藏行（Ctrl+Alt+9 / 右键菜单）
- sandbox 实现数据读写 + 底色 + 对齐 + 换行
- 已打包到 `outputs/meeting-scheduler.zip`
- Git 仓库：https://github.com/X-huaidan/meeting-scheduler

---

## 待发布（草稿）

> 每次加/删规则/功能时，agent 会**自动**追加一行到这里。
> 触发"更新上传 / 打包上传"或定时任务执行发版时，agent 会**自动**把下面这段合并到上方"已发布"段、并给本次发版指定一个版本号（次版本 +1），然后清空本段。

<!-- draft:start -->

<!-- draft:end -->
